#include <stdio.h>
#include "main.h"

/**
 * _strlen - Computes the lenth of a string
 * @s: string whose lenth is to be computed
 *
 * Return: Lenth of string (Success)
 */
int _strlen(char *s)
{
	int len, n, char s[];

	len = *(s + 1);
	return (len);
}
