#include <stdio.h>
#include "main"

/**
 * mul - Multiplies two integers
 * @a: Integer to be mulplied
 * @b: Integer to be multiplied
 *
 * Return: Result (SUccess)
 */
int mul(int a, int b)
{
	int result;
	
	result = a * b;
	return (result);
}
